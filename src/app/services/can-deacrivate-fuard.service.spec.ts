/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { CanDeacrivateFuardService } from './can-deacrivate-fuard.service';

describe('CanDeacrivateFuardService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [CanDeacrivateFuardService]
    });
  });

  it('should ...', inject([CanDeacrivateFuardService], (service: CanDeacrivateFuardService) => {
    expect(service).toBeTruthy();
  }));
});
